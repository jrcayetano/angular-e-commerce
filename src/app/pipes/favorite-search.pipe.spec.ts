import { FavoriteSearchPipe } from './favorite-search.pipe';

describe('FavoriteSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FavoriteSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
