import { UserLoggedActionsType } from './app-user-logged.actions';

export function clearState(reducer) {
  /*   return function(state,action) {
    if(action.type === UserLoggedActionsType.addFavoriteProduct) {
      state.credential = undefined;
      state.screenSubscription = undefined;

      sessionStorage.clear();
    }
    return reducer(state, action);
  } */
}
